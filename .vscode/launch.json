{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome against localhost",
      "url": "http://127.0.0.1:3001", // 修改为 127.0.0.1 避免可能的 DNS 解析问题
      "webRoot": "${workspaceFolder}"
    }
  ]
}